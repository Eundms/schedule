<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="toolbar_theme">@style/ToolbarTheme</item>
        <item name="toolbar_title_theme">@style/ToolbarTitleTheme</item>
        <item name="android:textColorPrimary">@android:color/black</item>
        <item name="android:textColorSecondary">@android:color/black</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="ToolbarTheme" parent="ThemeOverlay.AppCompat.ActionBar">
        <!-- if you have a navigationdrawer you change it color also -->
        <item name="colorControlNormal">@color/colorPrimary</item>
    </style>


    <style name="ToolbarTitleTheme" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <!-- Set title size -->
        <item name="android:textSize">@dimen/abc_text_size_title_material_toolbar</item>
        <!-- Set title color -->
        <item name="android:textColor">#FFFFFF</item>
    </style>

    <style name="ListTitleTheme" parent="TextAppearance.AppCompat">
            <item name="android:textColor">#5E05FB</item>
        <item name="android:textStyle">bold</item>
            <item name="android:textSize">20sp</item>
    </style>

    <style name="SpinnerTitleTheme" parent="TextAppearance.AppCompat">
        <item name="android:textColor">#5E05FB</item>
        <item name="android:textSize">20sp</item>
    </style>

</resources>
